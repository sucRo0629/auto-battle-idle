# Hensei-Only Agent Rules

このファイルは、このリポジトリで作業する AI エージェント向けの常設規則である。
ゲーム仕様そのものをここへ複製せず、判断に使う正本と作業手順を定める。

## 1. 判断の基本

- 一般的な RPG、既存作品、英語圏のクラス慣習から、仕様・役割・名称を補完しない。
- ユーザーの明示した決定を最優先し、設計書と現行実装を根拠として判断する。
- 兵科分類、役割、既存挙動、用語は効果の見た目から推測せず、正本を確認する。
- 不明な事項は創作しない。作業を左右しない場合は未確定として明示し、重大な分岐になる場合だけ確認する。
- 旧仕様は、その時点のクラスコンセプトに基づく設計資産として扱う。新案を出しただけで削除・置換対象にせず、変更には理由とユーザーの合意を必要とする。
- 回答と作業記録では、次の三つを混同しない。
  - **確定仕様**: 正本またはユーザーが確定した内容
  - **現行実装**: コード・データ・テストが現在行っていること
  - **新規提案**: まだ採用されていない案
- 正本間、正本と実装、または正本とユーザー決定に差がある場合、黙って整合させず差分を示す。

詳細: `.cursor/rules/project-principles.mdc`

## 2. 作業開始時の確認経路

タスクに必要な範囲だけを読むが、必要な責務を省略してはならない。

1. `docs/README.md` で正本とコードの対応を確認する。
2. フェーズ作業では、次を確認する。
   - `docs/ai-handoff/current-task.md`: 現在の具体的な作業範囲
   - `docs/plans/phase-roadmap.md`: フェーズ順と正式な境界
   - `docs/ai-handoff/planning-rules.md`: 完了条件と引き渡し規則
3. 設計判断では、該当する上位設計と spec を確認する。
   - 戦闘全体・Kill / Flow / Survival: `docs/combat-architecture.md`
   - クラス設計思想: `docs/class-philosophy.md`
   - 兵科・スキル・ゲーム用語: `docs/spec/classes-and-skills.md`
   - 戦闘処理・効果: `docs/spec/combat.md`
   - 共通戦闘メカニクス: `docs/system-mechanics.md`
4. 現行値や挙動を述べる場合は、対象データ、実行経路、validation、関連テストを確認する。
5. 対象文書が直接参照する関連 spec / plan も確認する。ヒットした節は、判断に必要な前後関係まで読む。

`current-task.md` と `phase-roadmap.md` が食い違う場合は、フェーズ順はロードマップ、具体的スコープは handoff を入口とし、矛盾を明示する。

全文一括読み込みは避ける。特に `data/skills/` は対象兵科の passive / active ファイルだけ、`data/classes.json` は ID 検索で確認する。ただし、トークン節約を理由に確認対象そのものを省かない。

詳細: `docs/README.md`、`.cursor/rules/documentation-index.mdc`、`.cursor/rules/data-json-lightweight.mdc`

## 3. 変更前の境界確認

- ユーザーが「まだ変更しない」「設計だけ」と言った場合、コード・データ・文書を変更しない。調査、比較、設計整理までを read-only で行う。
- named phase の変更前に、次を明示する。
  - 対象ファイルと責務
  - 受け入れ条件
  - 明示的なスコープ外
  - 正本
- 局所的な不具合や数値差を、その場限りのパッチで塞がない。再発する設計規則・責務・対象範囲を先に確認する。
- ユーザーの依頼と materially different な変更へ広げない。新しい権限、外部調整、設計範囲の拡張が必要なら止めて確認する。
- 既存の未コミット変更はユーザーの作業として保持し、無関係な差分を編集・削除・整形しない。

## 4. 設計提案の規則

- 単体効果だけでなく、クラスコンセプト、兵科間境界、同カテゴリ内の役割、内部シナジーまで確認する。
- 新パッシブは、兵科本体・既存パッシブ・CombatModuleとの関係を示す。構造が複雑なのに効果が地味、または説明量に対して判断価値が低い案を避ける。
- 他兵科固有の軸を流用するときは、役割侵食にならないか明示する。似たトリガー、蓄積、変換、低 HP 強化などは、既存所有者を確認してから提案する。
- 自動戦闘ゲームであることを前提にする。「手動発動」「プレイヤーがタイミングを選ぶ」など、存在しない操作価値を評価へ混ぜない。
- システム上同じ自動処理が複数ある場合、挙動・用語・表示で区別できるか確認する。
- ゲーム用語表にある固定語は表記を揺らさない。新しい用語を増やす前に既存語で表現できないか確認する。
- 仮名は仮名と明示する。日本語の意味とゲーム内挙動から先に考え、英語の直訳調、日常の健康管理語、挙動を誤認させる名称を避ける。数値・挙動が未確定なら命名を確定しない。
- 提案を整理するツリーや分類は、取得前提・排他関係を自動的に意味しない。取得ルールは確定仕様に明記された場合だけ付与する。

## 5. 数値・バランス評価

- コスト、レベル、確率、倍率は印象だけで評価せず、期待値、発動頻度、対象数、上限、オーバーヒール等の実効値を比較する。
- 同じカテゴリ内のパッシブは、同コスト帯で期待値の伸びが概ね平等か確認する。カテゴリを推測せず正本の分類を使う。
- 単体比較だけでなく、既存パッシブや兵科本体とのシナジー込みで評価する。
- 範囲効果、転送、連鎖、余剰量変換は、単体時と複数対象時を分けて評価する。
- 新しいコスト帯や取得条件は、将来候補と現試作の実装対象を区別する。設計候補に存在するだけで実装しない。
- 数値評価で確認すべき事項は、後工程へ送る場合も比較対象・指標・未確定点を具体的に残す。

## 6. 実装・文書・編集ツールの同期

- ゲームルール、データ形状、フェーズ計画を変更したら、`docs/README.md` の対応表に従い同じ作業内で正本を更新する。
- 数値や名称だけの変更では、正本が JSON なら spec に数値一覧を重複転記しない。ルール、意図、スキーマ、用語の変更だけを文書へ反映する。
- 新しい effect、target、条件、数値フィールド、表示要素を追加・変更した場合、runtime だけでなく schema、validation、normalize、editor、import/export、表示文、関連テストまで責務を確認する。
- `chain` など意味を持つデータ形状を、近い別形状で代用しない。要求された意味を schema と runtime の両方で保存する。
- legacy 共存は移行手段であり、新仕様の完了条件ではない。削除も温存も自動判断せず、現フェーズの境界に従う。

詳細: `.cursor/rules/documentation-sync.mdc`、`.cursor/rules/skill-data-editor-sync.mdc`

## 7. 検証と完了報告

- テスト緑だけで完了としない。確定仕様、production 経路、Player が到達する画面・導線との一致を確認する。
- 期待値を現行実装へ合わせるだけのテスト修正、失敗の skip、無関係なついで修正をしない。
- 検証は変更に比例した最小範囲から始め、必要に応じて広げる。
- `git diff --check` は基礎確認であり、型検査、 focused test、full suite、実画面確認の代替ではない。
- 報告では、実行した検証と、それぞれが保証する範囲を分ける。未確認範囲、既存失敗、Player 側未実装があれば明記する。
- 「Backend 完了」「Player 完了」「設計完了」を区別し、内部 API だけで Player 完了としない。

詳細: `.cursor/rules/tests-not-proof.mdc`、`docs/ai-handoff/test-policy.md`

### 完了判定の強制ゲート

- `.cursor/rules/instruction-compliance-gates.mdc` を常時適用する。
- 対象経路へ到達しなかった場合に成功する fallback、対象0件でも成功するループ、主要 assertion を通らない早期 return を検証として認めない。
- 完了報告では、各完了条件に対して確認経路・証拠・完了/未完了/未確認を対応づける。必須条件が未完了または未確認なら作業全体を完了と書かない。
- 比較用の変更前結果など根拠がない限り、失敗を `pre-existing` または今回差分由来ではないと断定しない。
- 個別プロンプトを作業終了時に再読し、禁止事項・スコープ外・次作業単位への先走りがないことを自己監査する。

## 8. 委譲・引き渡し

- 外部エージェント向けプロンプトにも、本ファイルの正本、推測禁止、文書同期、検証条件、スコープ外を具体的に含める。
- handoff には、対象ファイル、Backend / Player の完了条件、スコープ外、未実装事項、次の正式 Phase ID、確認済み対象と未確認対象を残す。
- 調査結果や提案を、確定仕様として handoff へ書かない。ユーザーが採用した内容だけを確定事項として記録する。

詳細: `.cursor/rules/project-implementation-prompts.mdc`、`docs/ai-handoff/planning-rules.md`

## 9. Windows 作業場所

- このリポジトリの絶対パスは `E:\Users\sucRo\Desktop\Hensei-Only`。
- コマンドの作業場所が無視される環境では、`git -C E:\Users\sucRo\Desktop\Hensei-Only ...` または絶対パスを使う。
- `C:\` や `E:\` 全体を検索しない。検索対象を必ずリポジトリ内へ限定する。
