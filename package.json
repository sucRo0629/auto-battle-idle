{
  "name": "auto-battle-idle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run --testTimeout=120000",
    "preview": "vite preview",
    "electron": "electron electron/main.mjs",
    "electron:dev": "npm run build && electron electron/main.mjs"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "happy-dom": "^20.10.6",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^3.2.4"
  }
}
